spring:
   datasource:
      username: chair_store
      password: chair_store
      url: jdbc:mysql://localhost:3306/chair_store
      
   jpa:
      hibernate:
         ddl-auto: none
      show-sql: true
     
   sql:
      init:
         mode: never